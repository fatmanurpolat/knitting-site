<!DOCTYPE html>
<html lang="<%= locale %>">
<head>
  <%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/nav') %>

  <main>
    <section class="page-hero">
      <div class="wrap page-hero__inner reveal">
        <p class="eyebrow"><%= t.bags.eyebrow %></p>
        <h1 class="display"><%= t.bags.title %></h1>
        <p class="lede"><%= t.bags.lede %></p>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <% if (products.length === 0) { %>
          <p class="empty"><%= t.bags.empty %></p>
        <% } else { %>
          <div class="grid grid--3">
            <% products.forEach(function (product) { %>
              <%- include('../partials/product-card', { product: product }) %>
            <% }); %>
          </div>
        <% } %>
      </div>
    </section>

    <section class="strip">
      <div class="wrap strip__inner strip__inner--center reveal">
        <p class="script strip__cta"><%= t.bags.stripCta %></p>
        <a class="btn" href="<%= href('/contact') %>"><%= t.bags.stripBtn %></a>
      </div>
    </section>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>

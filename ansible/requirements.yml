---
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix       # synchronize (rsync), firewalld helpers
    version: ">=1.5.0"
  - name: community.general   # ufw module
    version: ">=8.0.0"

# Copy to inventory.ini and fill in your server, then:
#   cd ansible && ansible-playbook playbook.yml
#
# The user needs sudo (become) rights to install packages and manage services.

[web]
orgulog ansible_host=203.0.113.10 ansible_user=ubuntu

[web:vars]
# Uncomment if you use a specific SSH key:
# ansible_ssh_private_key_file=~/.ssh/id_ed25519

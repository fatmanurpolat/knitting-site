# {{ ansible_managed }}
# Secret runtime configuration for {{ app_name }} (read by src/infrastructure/config.ts).
# Rendered from ansible/secrets.yml — file is root:{{ app_group }} mode 0640, NOT world-readable.
DATABASE_URL={{ database_url }}
ADMIN_USER={{ admin_user }}
ADMIN_PASSWORD={{ admin_password }}
SESSION_SECRET={{ session_secret }}
